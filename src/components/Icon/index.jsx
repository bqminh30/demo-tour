import React from "react";

const Icon = ({ icon }) => {
  return <div className="rounded-full cursor-pointer p-1.5 bg-main text-white flex justify-center items-center">{icon}</div>;
};

export default Icon;